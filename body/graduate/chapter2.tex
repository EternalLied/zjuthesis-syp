\chapter{面向一般拓扑的训练时间预测}
\section{引言}

在基于数据并行的分布式深度学习任务的训练过程中，每个计算节点（GPU）依次执行本地神经网络模型的计算和与其他节点的数据通信，这两部分共同决定了分布式训练所需的时间。通常情况下，加速比和扩展效率被用来衡量分布式训练的加速效果，其中加速比指同一深度学习任务在单机训练和分布式训练时所需时间的比值，扩展效率则是指加速比和节点数目的比值，也用于表示计算资源的利用率。随着节点数目的增加，通信时间在整体训练时间中的占比逐渐变大。当节点数目达到一定规模时，通信将成为性能瓶颈，从而导致加速比和扩展效率变差，资源利用率严重降低，这与我们使用分布式训练的初衷背道而驰。因此，需要一种工具帮助我们分析分布式训练的性能瓶颈，并确定合适的节点数目，在保证加速比的同时尽可能节约计算资源。

为了分析性能瓶颈，需要获得分布式训练每一环节的执行时间。考虑到在各种规模的GPU集群上实际部署分布式深度学习任务需要较高的成本，直接测量执行时间变得不可行。因此，预测分布式训练所需时间成为一种有效的解决方案。通过预测的方式，可以快速且低成本地分析性能瓶颈，进而确定一个相对高效的GPU集群规模用于部署分布式深度学习任务。

根据分布式训练的执行流程，分布式训练时间受到神经网络结构、GPU集群的拓扑结构、网络通信带宽等众多特征的影响，这些特征与训练时间之间往往呈现复杂的非线性关系，并且某些特征之间还存在耦合关系，从而大大增加建立准确的训练时间预测模型的难度。现有的方法虽然能够预测给定条件下的分布式训练时间，但是大都基于简单拓扑（星形拓扑或环形拓扑），并且其中一些方法还存在通信时间预测模型过于简单和没有考虑计算与通信之间耦合关系的问题。

因此，针对上述问题，本章提出了一种预测一般拓扑上分布式深度学习任务训练时间的方法。首先，对训练时间进行建模并详细分析了影响分布式深度学习训练时间的特征。然后，分别建立了基于MLP的计算时间预测模型和基于机理分析的通信时间预测模型。其中在计算时间预测部分，本章通过分析神经网络的计算原理引入了额外的特征，进一步提高了预测精度，另外还建立了不同神经网络层的基准测试数据集用于训练对应的MLP模型。在通信时间预测部分，本章通过分析深度学习框架中集合通信原语的执行机理和拓扑结构的通信特征建立了细粒度的通信时间预测模型，该模型适用于各种参数同步拓扑上的通信时间预测。为了提高预测精度，本章还通过分析计算与通信的并行关系提取了分布式训练的关键路径，用于预测训练时间。最后，通过与真实GPU集群上测试的分布式训练卷积神经网络模型所需训练时间的比较验证了本章提出方法的有效性。

本章其他小结的内容安排如下：第2.2节对分布式深度学习的训练时间进行建模并分析了影响训练时间的特征；第2.3节介绍了本章提出的分布式深度学习训练时间预测方法的各部分；第2.4节基于真实测量结果对本章所提出方法的效果进行验证分析；第2.5节对本章内容进行了总结。

\section{问题描述}
\subsection{分布式深度学习训练时间建模}

在深度学习中，数据集通常是以某个批量大小按批次输入到神经网络中进行训练的，因此训练时间$T$可以表示为每次迭代所需时间$T_{iter}$与总迭代次数$c_{iter}$的乘积：
\begin{equation}
    T=T_{iter}c_{iter},
\end{equation}
其中总迭代次数为处理整个数据集所需迭代次数（通常称为一个训练周期）与训练周期数的乘积。由于深度学习任务的训练通常采用固定的训练周期数，如100、200等，并且当批量大小给定时，一个训练周期的迭代次数也随之确定，因此预测分布式深度学习的训练时间通常指预测其一次迭代所需的训练时间。

如\autoref{fig:Fig2-1}所示，基于数据并行的分布式训练的每次迭代由三个关键步骤组成。首先，每个节点执行一个数据拷贝过程。因为神经网络每次迭代训练一个批量的数据样本，该过程则是将每次迭代的样本从CPU的动态随机存取存储器（DRAM）拷贝到GPU的视频随机存取存储器（VRAM），从而用于GPU上模型的训练。然后，每个节点并行执行本地神经网络模型的前向传播、反向传播和模型参数更新过程。接下来，各节点之间交换更新后的模型参数，以实现参数同步。因此，分布式训练的迭代时间包括：（1）数据拷贝时间；（2）节点本地计算时间；（3）节点之间的通信时间。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{figure/chapter2/fig2-1-training_step.pdf}
    \caption{\label{fig:Fig2-1}分布式训练的执行流程}
\end{figure}

根据上述训练过程，本文将分布式训练的迭代时间建模为如下数学表达形式：
\begin{equation}
    \label{tab:iter_time_fomula}
    \begin{aligned}
        &T_{iter}=T_{copy}+f(T_{comp},T_{comm}),\\
        &T_{comp}:=\left( T_{comp}^{layer_1}(\boldsymbol{c}_{nn}^{layer_1},\boldsymbol{c}_{gpu}),...,T_{comp}^{layer_l}(\boldsymbol{c}_{nn}^{layer_l},\boldsymbol{c}_{gpu}) \right), \\
        &T_{comm}:=\left( T_{comm}^{layer_1}(\boldsymbol{c}_{cluster}),...,T_{comm}^{layer_l}(\boldsymbol{c}_{cluster}) \right).
    \end{aligned}
\end{equation}
其中$T_{copy}$是数据拷贝时间，$T_{comp}$和$T_{comm}$分别是各神经网络层的计算时间和通信时间的集合，$l$是神经网络的层数，$\boldsymbol{c}_{nn}^{layer_i}$是第$i$个神经网络层的特征，$\boldsymbol{c}_{gpu}$是GPU的硬件特征，$\boldsymbol{c}_{cluster}$是GPU集群的通信特征。在大多数深度学习框架中，为了充分利用计算资源和通信带宽并加快分布式训练的速度，通常将计算和通信并行执行\cite{pei2019iteration, lin2020topology, shi2018performance}，本章用$f$来表征这一并行关系。由于数据拷贝时间可以直接利用每个批量的数据样量除以CPU和GPU之间的通信带宽得到，并且该时间在每次迭代中通常保持不变，因此本章的主要目标是预测\autoref{tab:iter_time_fomula}中的$f(T_{comp},T_{comm})$。

\subsection{计算时间关键特征分析}

分布式训练每次迭代的计算时间主要由神经网络结构和GPU的硬件性能决定。本章以近年来在图像领域广泛使用的卷积神经网络（Convolutional Neural Network，CNN）为例，分析了该类神经网络中常见的卷积层、池化层、全连接层和批归一化层的主要特征，并通过机理分析进行了特征扩充。由于大多数神经网络层的计算可以看作矩阵相乘，因此本章的分析和建模方法同样适用于其他基于矩阵相乘的神经网络层。
%分析了该类神经网络中常见的卷积层、全连接层、池化层和批归一化层的主要特征

卷积神经网络\cite{lecun2010convolutional}是一类在计算机视觉和模式识别领域取得显著成功的深度学习模型。其设计灵感源于生物学中对动物视觉系统的理解，通过层层堆叠的卷积层、池化层和全连接层，实现对输入数据的逐层抽象和表示学习。其大致结构如\autoref{fig:Fig2-2}所示。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.7\linewidth]{figure/chapter2/fig2-2-cnn-architecture.png}
    \caption{\label{fig:Fig2-2}卷积神经网络结构示意图}
\end{figure}

卷积层是卷积神经网络的核心组件之一，其主要作用是通过局部感受野内的权重共享卷积操作，从输入数据中提取特征。卷积操作本质上是一种线性图像滤波过程，在该过程中，首先选择具有特定规格大小的卷积核，卷积核的数量决定了卷积后产生的特征图的通道数量。随后，对特征图周围添加额外的像素值（padding）以避免边缘信息的丢失。接下来，卷积核以预定的步长对输入特征图进行从左到右、从上到下的扫描。在每次扫描中，卷积核上的数值与相应位置的图像像素值相乘，并将结果求和。最终，计算结果被映射到输出特征图上的对应位置，形成输出特征图。

池化层是卷积神经网络的另一核心组件，又被称为降采样层，其主要作用是通过使用池化核对卷积层输出的特征图进行下采样，从而降低特征图的维度，减少模型过拟合的风险。在池化操作中，池化核沿着输入数据滑动，将每个池化区域内的数值进行聚合，生成一个新的特征值，从而形成输出的池化特征图。最常见的池化操作有最大池化和平均池化两种。在最大池化中，每个池化区域的最大值被选为该区域池化后的特征采样值；而在平均池化中，池化区域的平均值被作为特征采样值。

全连接层是卷积神经网络的一个关键组件，用于整合和提取高阶特征，起到全局感知的作用。在全连接层中，每个神经元与前一层的所有神经元相连接，形成全连接的网络结构。这种结构特点使得全连接层的参数量在卷积神经网络中占很大比重。

批归一化层是卷积神经网络中的一项重要技术，其原理在于对每个批次的输入数据进行归一化处理，以加速网络的训练过程并提高模型的泛化性能。在批归一化中，每个特征通道的均值和方差被计算，并通过缩放和平移操作调整数据分布，使其保持在较稳定的范围内。

此外，由于激活函数和优化器分别在卷积神经网络的前向传播和反向传播过程其中起到增强卷积神经网络的非线性表达能力和调整神经网络的权重的作用，这两个特征也被用于预测模型的建立。在考虑众多GPU硬件性能对计算时间的影响时，本文主要关注GPU内存大小、GPU内存带宽、GPU浮点数运算性能、GPU内核数和GPU时钟频率这几个特征。综上所述，影响卷积神经网络计算时间的基本特征如\autoref{tab:features-of-CNN}所示。

\begin{table}[!htbp]
\center
\caption{影响卷积神经网络计算时间的基本特征}
\label{tab:features-of-CNN}
\setlength{\tabcolsep}{3mm}{ %可随机设置，调整表格整体长度到适合自己的大小为止
\scalebox{0.9}{
    \begin{tabular}{llp{11cm}}
      \hline
      特征          & 符号 & 描述 \\ 
      \hline
      输入尺寸        &  $m_{in}$      &  卷积层/池化层/批归一化层的输入特征图的宽度和高度。 \\
      输出尺寸       & $m_{out}$      &  卷积层/池化层/批归一化层的输出特征图的宽度和高度。 \\
      批大小               &     $b$        &  一次迭代训练的样本数。 \\
      输入通道数            &   $c_{in}$     &  卷积层/池化层/批归一化层的输入特征图的通道数。  \\
      输出通道数           &     $c_{out}$  &  卷积层/池化层/批归一化层的输出特征图的通道数。 \\
      卷积核/池化核大小              &     $k$        &  卷积层/池化层的卷积核/池化核的宽度和高度。          \\
      边界填充大小                 &     $p$        &  在卷积层/池化层的输入特征图的周围添加的额外边界大小。 \\
      步幅大小                  &     $s$        & 卷积核/池化核在水平和垂直方向上每次移动的距离            \\
      池化层类型             &     -           &  池化层的类型，包括最大池化、平均池化等。            \\ 
      偏置项                     &     $bias$          & 每个神经元的输出上添加的常数值。            \\
      激活函数类型        &     -          &  激活函数的类型，包括ReLU、Sigmoid、Tanh等。           \\
      优化器类型                &     -          & 模型参数更新使用的算法，包括随机梯度下降（SGD）、动量优化（Momentum）等。             \\
      输入维度          &    $d_{in}$    &  全连接层的输入特征维度。           \\
      输出维度         &    $d_{out}$   &  全连接层的输出数据维度。           \\
      GPU内存大小               &    -           &  GPU的可用内存容量，也成为显存。           \\ 
      GPU内存带宽     & -              &  GPU上的内存系统传输数据的速度，单位GB/s。 \\
      GPU浮点数运算性能 &      -   &  GPU每秒能够执行的浮点运算次数（FLOPS）。  \\
      GPU内核数          &        -       &  GPU中的处理单元数量。 \\
      GPU时钟频率          &       -        &  GPU的处理器核心运行时的时钟速率，单位Hz。   \\
      \hline
    %   \hline
    %   Communication Data Size    &        -     &  The size of data communicated between GPUs.\\
    %   Communication Bandwidth    &       -     &  The maximum number of floating point numbers that can be transmitted per second. \\
    %   Number of GPUs    &        -     &  The number of GPUs used for distributed training.\\
    %   Communication Topology    &        -     &  The topology describes which neighbors each node can communicate with.\\
    %   Consensus Protocol    &        -     &  Data exchange protocol used in distributed training.\\
    %   Physical Link Type    &        -     &  Transmission path of data communicated between GPUs.\\
    %   \hline
    \end{tabular}
}}
\end{table}

除了\autoref{tab:features-of-CNN}中展示的基本特征，本章还通过分析卷积神经网络的执行机理引入了一些影响计算时间的关键特征，以提高预测模型的精度。这些特征包括卷积核的内存占用量$mem_w$，卷积层输入数据的内存占用量$mem_{in}$和输出数据的内存占用量$mem_{out}$，卷积层的浮点数运算次数$ops_{conv}$以及全连接层的浮点数运算次数$ops_{fc}$。基于\autoref{tab:features_fomula}可以得到这些额外引入的关键特征：
\begin{equation}
    \label{tab:features_fomula}
    \begin{aligned}
        &mem_w=\begin{cases}
            k^2c_{in}c_{out}+c_{out},&bias=1\\
            k^2c_{in}c_{out},&bias=0\\
        \end{cases},\\
        &mem_{in}=bm_{in}^2c_{in}, \hspace{0.2in} mem_{out}=bm_{out}^2c_{out},\\
        &m_{out}=\lfloor \frac{m_{in}+2p-k}{s} \rfloor +1,\\
        &ops_{conv}=bm_{out}^2k^2c_{in}c_{out}, \hspace{0.2in} ops_{fc}=bd_{in}d_{out}.
    \end{aligned}
\end{equation}
其中各个符号的定义可以在\autoref{tab:features-of-CNN}中找到。

\subsection{通信时间关键特征分析} \label{section2-2-3}

分布式训练每次迭代的通信时间主要由GPU集群的通信配置决定，包括通信数据量、节点（GPU）之间的通信带宽、节点的数目、集群拓扑结构、分布式训练架构和连接GPU的物理链路类型。下面具体分析这些因素对通信时间的影响。

一般而言，分布式训练的通信时间随通信数据量的增加而增大。不过其变化关系并不是线性的，因为真实的通信带宽并不是固定且等于最大通信带宽的，而是会随着通信数据量变化。本章将真实的通信带宽称为实时吞吐量。如\autoref{fig:Fig2-4}所示，在实际测量中，实时吞吐量与通信数据量的对数之间呈现类似Sigmoid函数的关系。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.7\linewidth]{figure/chapter2/fig2-4-throughput.pdf}
    \caption{\label{fig:Fig2-4}实时吞吐量随通信数据量的变化}
\end{figure}

节点数目对通信时间的影响取决于分布式训练架构和集群的拓扑结构。在参数服务器（Parameter Server，简称PS）架构中，每个工作者节点需要将数据发送给参数服务器节点，因此通信时间会随着工作者节点数目的增加而增大，进而导致参数服务器节的通信开销成为性能瓶颈。在全归约（All-Reduce）架构中，以Ring All-Reduce算法为例（算法细节在\autoref{subsection_2_3_3}中），随着节点数目的增加，数据传输时间将先增加然后趋于稳定，从而实现数据传输时间几乎不依赖于节点数目，但由于Ring All-Reduce的实现需要数据在各节点间依次传递，因此通信时延将随着节点数目线性增加。这里的通信时延指数据传输时间之外的通信时间，包括排队延迟、通信协议开销等。在去中心化的分布式（Gossip）架构中，由于每个节点只与邻居节点进行通信，所以通信时间与拓扑结构密切相关，具体而言，每次迭代的通信时间取决于拓扑中节点的最大度。并且通常情况下，节点数目越多，拓扑越稠密，通信时间越大。

在GPU之间进行通信时，不同类型的物理链路会导致不同的通信带宽，进而影响通信时间。在本章中，我们主要考虑如\autoref{fig:Fig2-3}所示的标准服务器架构中常见的三种物理链路类型：PIX、NODE和SYS。其中PIX指挂载在同一个PCIe Bridge（Switch）上的两个GPU之间的通信链路，相当于图中的GPU1到GPU2；NODE指单个非一致性内存访问节点（NUMA node）内经过Host Bridge PCIe总线的通信链路，相当于图中的GPU1到GPU3；SYS指通过QPI（PCIe+QPI总线）跨NUMA node间GPU通信的链路，相当于图中的GPU1到GPU5。对于其他类型的链路，例如跨服务器的链路、NVLink等，本章的分析和建模方法也同样适用，但是由于用于实验测试的系统的限制，本章的介绍中不涵盖其他类型的物理链路。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.8\linewidth]{figure/chapter2/fig2-3-server_architecture.pdf}
    \caption{\label{fig:Fig2-3}标准服务器架构}
\end{figure}


\section{分布式深度学习训练时间预测方法}
\subsection{方法概述}

本章提出了一种新的训练时间预测方法，将其称为面向一般拓扑的分布式深度学习训练时间预测（Training Time Prediction of Distributed Deep Learning over General Topology，PreTopo）。\autoref{fig:Fig2-5}为PreTopo的框架图，可以看到，PreTopo主要由三个部分组成：
\begin{itemize}
    \item 计算时间预测。由于神经网络的不同层和模块通过直接连接进行交互，同时保持计算和功能的独立性，因此PreTopo将神经网络模型分解为单个神经网络层的组合，逐层预测神经网络的计算时间。在预测模型的建立过程中，首先对每种类型的神经网络层的特征空间进行采样，然后根据采样的特征搭建单层神经网络并测试其计算时间。接下来使用\autoref{tab:features_fomula}对特征进行扩充，并将每种类型的神经网络层的特征和对应的测试时间作为基准测试数据集，用于训练特定的多层感知器模型（MLP）。最后使用这些多层感知器模型即可预测一个新的神经网络的计算时间。
    \item 通信时间预测。首先分析实时吞吐量随通信数据量的变化并建立单链路通信时间预测模型。然后基于对分布式训练中常用的集合通信原语的执行机理的分析，结合单链路模型建立多GPU通信时间预测模型。最后通过分析在特定分布式训练架构上利用各种拓扑结构进行通信所需的通信次数以及每次参与通信的GPU集合，并利用已经建立的单链路模型和多GPU模型，建立适用于一般拓扑的通信时间预测模型。
    \item 计算与通信的并行性分析。在得到各神经网络层的计算时间和通信时间之后，PreTopo为集群中的每个节点和每条边建立对应的操作队列和设备时间，并根据分布式训练的执行流程依次将每个计算操作和通信操作加入到其操作队列中，然后更新相应的设备时间。最后基于每个操作开始执行和结束执行的设备时间提取关键路径，进而预测分布式训练的迭代时间。
\end{itemize}

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.8\linewidth]{figure/chapter2/fig2-5-framework.pdf}
    \caption{\label{fig:Fig2-5}PreTopo框架图}
\end{figure}

\textcolor{red}{此处补充一个算法流程图}
\begin{center}
% \begin{minipage}{8.5cm}
\begin{algorithm}[!htbp]
      \caption{\textbf{PerfTop}: \underline{Perf}ormance prediction framework over general \underline{Top}ologies} 
      % \small
      \label{Algorithm-PreTopo} 
        \begin{algorithmic}[1]
        \REQUIRE{The depth of neural network $l$; The type of each neural network layer $layer_i, i\in{1,...,l}$; The set of features for each neural network layer $\boldsymbol{c}_{nn}^{layer_i}$; The set of hardware features of GPU $\boldsymbol{c}_{gpu}$; The set of communication features of the cluster $\boldsymbol{c}_{cluster}$}.
        \ENSURE{The iteration time of distributed training $T_{iter}$}.
        \FOR{each layer $i \in [1, l]$}
            \STATE Calculate the expanded features of $layer_i$ using Eq.~\eqref{tab:features_fomula}
            \begin{align*}
            \small
                &mem_w=\begin{cases}
                    k^2c_{in}c_{out}+c_{out},&bias=1\\
                    k^2c_{in}c_{out},&bias=0\\
                \end{cases}\\
                &mem_{in}=bm_{in}^2c_{in}, \hspace{0.2in} mem_{out}=bm_{out}^2c_{out},\\
                &m_{out}=\lfloor \frac{m_{in}+2p-k}{s} \rfloor +1,\\
                &ops_{conv}=bm_{out}^2k^2c_{in}c_{out}, \hspace{0.2in} ops_{fc}=bd_{in}d_{out}.
            \end{align*}
            
            \STATE Predict computation time using the MLP model.
        \ENDFOR
        \STATE \textbf{end for}
        \IF{data communication is realized by \texttt{distributed.} \texttt{all\underline{~}reduce}}
        \STATE Predict the communication time using Equation
        \begin{align*}
        \small
            t_{all\underline{~}reduce}^{cluster}=\left( m-1 \right) \cdot t_{all\underline{~}reduce}^{link}\left( \frac{2N}{m} \right),
        \end{align*}
        \ELSE
        \STATE Obtain the number of communications $c$ and the set of communication links $links_i$ based on consensus protocol.
        \STATE Predict the communication time using Equation
        \begin{align*}
        \small
            t_{comm}=\sum_{i=1}^c{t_{broadcast}^{cluster}\left( n_i, links_i \right)},
        \end{align*}
        \ENDIF
        \STATE \textbf{end if}
        \STATE Extract the critical path and compute the iteration time.
        \end{algorithmic}
\end{algorithm}
% \end{minipage}
\end{center}

\subsection{计算时间预测模型}

在本章中，用于预测每个神经网络层计算时间的多层感知器模型由五个全连接层组成，其中前四个全连接层为隐藏层，用于学习输入特征之间的关系，最后一个全连接层为输出层，用于学习隐藏层特征与输出之间的关系，每个全连接层执行如下操作：
\begin{equation}
    \label{tab:MLP-layer}
    Y=W\cdot X+b,
\end{equation}
其中，$W$是全连接层的权重矩阵，$b$是偏置参数，$Y$是全连接层的输出数据。本章中全连接层的神经元数目分别为32、64、128和128，输出层神经元数目为1，表示预测的计算时间。其中，每个隐藏层的输出会经过ReLU激活函数，其表达式如\autoref{tab:ReLU}所示，它保证了整个神经网络是非线性且稀疏的：
\begin{equation}
    \label{tab:ReLU}
    ReLU\left( Y \right) =\begin{cases}
        Y,if\,\,Y>0\\
        0,if\,\,Y\leqslant 0\\
    \end{cases}.
\end{equation}

在训练时，输出层经过ReLU激活函数之前会以20\%的概率对其执行dropout操作以减轻过拟合。构建的多层感知器模型如\autoref{fig:Fig2-6}所示。针对不同类型的神经网络层，多层感知器的输入层神经元数目有所不同。根据卷积层、池化层、全连接层和批归一化层的特征分析结果，其输入层神经元数目分别为19、12、11和10。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.6\linewidth]{figure/chapter2/fig2-6-MLP.pdf}
    \caption{\label{fig:Fig2-6}多层感知器模型}
\end{figure}

在建立每种类型神经网络层对应的MLP模型之后，通过特征空间采样并测试得到基准测试数据集以训练MLP模型（基准测试数据集获取细节见\autoref{subsection_2_4_1}）。进而利用训练好的模型即可对待预测神经网络的计算时间进行逐层预测，并得到其单次迭代的计算时间：
\begin{equation}
    \label{tab:computation_time}
    T_{comp}^{iter}=\sum_{i=0}^l{T_{comp}^{layer_i}}\left( c_{nn}^{layer_i},c_{gpu} \right) =M_{type_i}\left( c_{nn}^{layer_i},c_{gpu} \right) ,
\end{equation}
其中$l$是神经网络的层数，$\boldsymbol{c}_{nn}^{layer_i}$是第$i$个神经网络层的特征，$\boldsymbol{c}_{gpu}$是GPU的硬件特征，$type_i$是第$i$个神经网络层的类型，$M$是建立的MLP模型。

\subsection{通信时间预测模型}\label{subsection_2_3_3}

\textbf{1. 单链路通信时间预测模型}

本节首先对分布式深度学习中常用的两种集合通信原语，全归约（$all\underline{~}reduce$）和广播（$broadcast$），建立了单链路通信时间预测模型。与Qi等人\cite{qi2016paleo}直接使用固定带宽预测通信时间的方法不同，我们通过测试发现，真实的通信带宽（又称实时吞吐量）会随着通信数据量而改变。因此，我们先使用最小二乘法拟合实时吞吐量和通信数据量之间的关系，然后基于拟合得到的实时吞吐量模型建立单链路通信时间预测模型。

对于$all\underline{~}reduce$，实时吞吐量与通信数据量的对数之间呈现类似Sigmoid函数的关系，即满足：
\begin{equation}
    tp_{all\underline{~}reduce}=c\cdot \frac{1}{e^{-a\cdot \log \left( N \right)}+b},
\end{equation}
其中$tp_{all\underline{~}reduce}$是实时吞吐量，$N$是通信数据量，$a$，$b$和$c$是Sigmoid模型的拟合参数。接下来，通信时间可通过下式得到：
\begin{equation}
    \label{tab:single_link_allreduce}
    t_{all\underline{~}reduce}^{link}=\frac{N}{tp}.
\end{equation}

对于$broadcast$，当通信数据量小于阈值$th$时，实时吞吐量与通信数据量的对数之间呈近似线性关系，因此我们建立了两阶段模型以进一步提高通信时间预测精度。于是，基于$broadcast$的单链路通信时间预测模型如下：
\begin{equation}
    \label{tab:single_link_broadcast}
    t_{broadcast}^{link}=\left\{ \begin{matrix}
        p\log \left( N \right) +q&		N<th\\
        N/\left( \,\,c\,\,\cdot \,\,\frac{1}{e^{-a\cdot \log \left( N \right)}+b} \right)&		N\geqslant th\\
    \end{matrix} \right. ,
\end{equation}
其中，$p$和$q$是第一段线性模型的拟合参数。

\textbf{2. 多GPU通信时间预测模型}

基于$all\underline{~}reduce$的多GPU通信底层是采用Ring All-Reduce算法实现的。Ring All-Reduce算法是一种在环形集群拓扑上实现全局数据通信的方法，该算法通过多轮通信使得每个节点最终得到所有节点的数据，包括分散规约（scatter-reduce）和全收集（all-gather）两个阶段。

首先是scatter-reduce，在该阶段中，GPU集群会逐步交换彼此的数据并融合。在该阶段结束时，每个GPU节点都会得到完整融合的数据的一部分，即最终结果的一个数据块。在开始通信之前，每个节点会先根据节点数目$n$将要通信的数据按维度切分为$n$个数据块。例如，对于第$i$个节点，其数据可以表示为$x_i=\left( x_{i}^{1},x_{i}^{2},...,x_{i}^{n} \right)$。接下来执行$n-1$次数据交换，在第k次数据交换时，第$i$个节点会将其第$(i-k)\%n$个数据块发送给下一个节点，并在接收到上一个节点的数据块后，将其与自己对应位置的数据块求和。\autoref{fig:Fig2-7}为scatter-reduce的执行流程。

然后是all-gather，在该阶段中，GPU集群会逐步交换彼此完整融合的数据块。在该阶段结束时，所有GPU最终都会得到完整融合的数据。如前文所述，在执行完scatter-reduce后，每个节点的数据里都有一个数据块是所有节点对应位置数据块求和的结果。现在需要将这些数据块发送到每个节点上，和scatter-reduce一样，也需要$n-1$次数据交换，不过在每次数据交换后不是执行数据块求和，而是执行数据块覆盖。具体而言，在第$k$次数据交换时，第$i$个节点会将其第$(i+1-k)\%n$个数据块发送给下一个节点，并在接收到前一个节点的数据块后，利用该数据块覆盖自己对应位置的数据块。\autoref{fig:Fig2-8}为all-gather的执行流程。

\begin{figure}[!htbp]
    \centering
    \subfigure[1次scatter-reduce操作]{
        \includegraphics[width=0.48\linewidth]{figure/chapter2/fig2-7-1-scatter-reduce-1.pdf}
        \label{fig:Fig2-7-scatter-reduce-1}%文中引用该图片代号
    }
    \subfigure[scatter-reduce阶段结束]{
        \includegraphics[width=0.48\linewidth]{figure/chapter2/fig2-7-2-scatter-reduce-2.pdf}
        \label{fig:Fig2-7-scatter-reduce-2}%文中引用该图片代号
    }
    \caption{Ring All-Reduce的scatter-reduce操作}
    \label{fig:Fig2-7}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \subfigure[1次all-gather操作]{
        \includegraphics[width=0.48\linewidth]{figure/chapter2/fig2-8-1-all-gather-1.pdf}
        \label{fig:Fig2-8-all-gather-1}%文中引用该图片代号
    }
    \subfigure[all-gather阶段结束]{
        \includegraphics[width=0.48\linewidth]{figure/chapter2/fig2-8-2-all-gather-2.pdf}
        \label{fig:Fig2-8-all-gather-2}%文中引用该图片代号
    }
    \caption{Ring All-Reduce的all-gather操作}
    \label{fig:Fig2-8}
\end{figure}

由上述介绍可知，Ring All-Reduce算法的两个阶段都需要$n-1$次通信，并且在每次通信时，每个节点从它的前一个邻居接收数据量为$N/n$的数据，同时将数据量为$N/n$的数据发送给它的后一个邻居。基于前面建立的单链路模型，我们可以推导出在由$m$个GPU组成的集群中使用$all\underline{~}reduce$进行通信所需的时间为：
\begin{equation}
    \label{tab:multi_gpu_allreduce}
        t_{all\underline{~}reduce}^{cluster}=\left( m-1 \right) \cdot t_{all\underline{~}reduce}^{link}\left( \frac{2N}{m} \right),
\end{equation}
其中$t_{all\underline{~}reduce}^{link}$是具有最小实时吞吐量的链路的通信时间。

另外，我们还利用单链路通信时间的建模方法建立了基于小规模GPU集群的通信时间预测模型，并将其扩展到相对更大的GPU集群。这里扩展的前提是保证建模所使用的小规模GPU集群中的物理链路类型与待预测的更大规模GPU集群中的物理链路类型相同。基于该建模方法，我们可以得到新的通信时间预测模型：
\begin{equation}
    \label{tab:multi_gpu_allreduce_based_multiGPU}
        t_{all\underline{~}reduce}^{cluster}=\frac{m-1}{s-1}\cdot t_s\left( \frac{sN}{m} \right),
\end{equation}
其中$s$是建模所使用的GPU数目。我们在\autoref{subsection_2_4_3}中对\autoref{tab:multi_gpu_allreduce}和\autoref{tab:multi_gpu_allreduce_based_multiGPU}的预测结果进行了对比分析。

基于我们的分析方法，只要为每种类型的物理链路建立基于$all\underline{~}reduce$的单链路通信时间预测模型（物理链路类型通常为\autoref{section2-2-3}中提到的五种类型），即可得到在任意规模的GPU集群上使用$all\underline{~}reduce$进行通信所需的时间。

与$all\underline{~}reduce$不同，基于$broadcast$的通信主要用于将一个节点的数据发送给其他节点。传统的$broadcast$操作是利用链式拓扑将待通信的数据从源节点依次发送给每个节点，因此通信时间会随着GPU的数目线性增加。为了提高通信效率，英伟达提出的NCCL（Nvidia Collective multi-GPU Communication Library）通信库对$broadcast$的通信模式进行了改进。如\autoref{fig:Fig2-9}所示，在开始通信之前，源节点将待通信的数据切分为$S$个数据块，然后从源节点开始依次将$S$个数据块发送给下一个节点。即在第$k$次通信时，第$i\left(\max \left( 2, k-S+2 \right) \leqslant i\leqslant \min \left( k+1, n-1 \right)\right)$个节点从第$i-1$个节点接收第$k+2-i$个数据块，并将第$k+1-i$个数据块发送给第$i+1$个节点，如此进行$S+n-2$次通信使得每个节点最终都接收到完整的数据。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.5\linewidth]{figure/chapter2/fig2-9-broadcast.png}
    \caption{\label{fig:Fig2-9}基于$broadcast$的通信流程}
\end{figure}

基于上述broadcast的原理，我们建立如下多节点broadcast通信时间预测模型：
\begin{equation}
    \label{tab:multi_gpu_broadcast}
        t_{broadcast}^{cluster}=\left( S+n-2 \right) t_{broadcast}^{link}\left( \frac{N}{S} \right).
\end{equation}
考虑到不同GPU之间可能经过不同的物理链路进行通信，例如\autoref{fig:Fig2-4}中有三种不同的物理链路，并且这三种链路对应着不同的通信带宽。因此，我们将\autoref{tab:multi_gpu_broadcast}进一步精细化为：
\begin{equation}
    \small
    \label{tab:multi_gpu_broadcast_refined}
    \begin{aligned}
        t_{broadcast}^{cluster}\left( n, links \right) =&\left( S-1 \right) \cdot t_{broadcast}^{link_j}\left( \frac{N}{S} \right)+\sum_{link_i\in links}{t_{broadcast}^{link_i}\left( \frac{N}{S} \right)}\\t_{broadcast}^{link_j}=&\underset{link_i\in links}{\max}t_{broadcast}^{link_i},
    \end{aligned}
\end{equation}
其中$links=\left\{ link_1,..., link_{n-1} \right\}$是参与通信的所有物理链路的集合。

\textbf{3. 一般拓扑上的集群通信时间预测模型}

由于基于$all\underline{~}reduce$的通信采用固定的环形拓扑，因此在分布式深度学习中，各种通信拓扑上的训练通常是基于$broadcast$来实现通信。利用上一小结中建立的单链路通信时间预测模型和多GPU通信时间预测模型，参考项目结题文档

然后讲Gossip架构和All-Reduce架构分别进行1次通信和多次通信。

\subsection{计算与通信的并行性分析}

\section{算力分析}
\subsection{数据准备}\label{subsection_2_4_1}
\subsection{实验设置与评价指标}
\subsection{实验结果与分析}\label{subsection_2_4_3}


\section{本章小结}


