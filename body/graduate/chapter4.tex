\chapter{分布式训练中的计算通信并行优化方法}
\section{引言}

当前，以AIGC为代表的大模型得到广泛的应用和部署，其参数可达到万亿级别，并且仍然保持迅速增长的趋势。随着模型迅速增大，大部分大模型采用了跨节点分布式部署的方式。在跨节点部署场景中，节点间需要频繁执行各种集合通信算子来协同训练，导致通信时间在整个训练过程中占据了相当的比例。因此，能否通过计算与通信的并行优化来通信开销，是加速大模型分布式训练至关重要的瓶颈点。

分布式训练任务中，常用的并行训练方式有数据并行（Data Parallelism，DP）、张量并行（Tensor Parallelism，TP）和、流水并行（Pipeline Parallelism，PP）和专家并行（Expert Parallelism，EP）等。
在数据并行中，每个节点持有整个模型的副本，并在划分的本地数据集上进行训练，各本地梯度通过例如AllReduce的集合通信方式进行聚合，然后更新参数，以保持不同节点模型间的同步。然而，数据并行可能无法满足大规模模型的训练需求，这些模型可能超出设备的存储和计算能力，因此，模型并行的使用变得不可避免。模型并行包括张量并行、流水并行和专家并行等方式。
张量并行是一种为Transformer模型的分布式训练提出的优化策略，它将权重矩阵在行或列维度上进行划分，并依靠AllReduce操作确保划分前后的数学等效性。张量并行可以有效减少单个节点的显存占用，但是节点间需要进行频繁的通信同步参数。
流水并行则将神经网络模型切分为多个阶段，并将这些阶段分配到不同的计算节点上，每个节点处理模型的一个子集，并在前向和反向传播过程中与其他节点进行通信。流水并行存在着Bubble现象，即在某些时间段内，部分节点处于空闲状态，无法充分利用计算资源。为了解决这一问题，可以采用微批次（Micro-batch）技术，将输入数据划分为更小的批次，并在流水线中交错处理这些微批次，从而提高资源利用率和整体训练效率。
专家并行是一种针对混合专家模型（Mixture of Experts，MOE）提出的并行策略，它将模型中的不同专家模块分配到不同的计算节点上，能够实现高效的模型训练和推理。
各种并行训练模式示意图如\autoref{fig:parallel_pattern}所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.8\linewidth]{figure/chapter3/parallel_pattern.pdf}
    \caption{\label{fig:parallel_pattern}分布式训练中的常见并行模式示意图}
\end{figure}

实际训练任务中，通常会结合多种并行方式以充分利用计算资源并提升训练效率。例如，在大模型训练中，数据并行常与张量并行或流水并行结合使用，以应对模型规模和计算需求的挑战。图\autoref{fig:3D-parallel}给出了数据并行、张量并行、流水并行同时部署的3D并行模式。在这种多并行方式结合的场景下，计算与通信的并行优化变得尤为重要，因为不同并行方式下的通信模式和需求各不相同。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.6\linewidth]{figure/chapter3/3D-parallel.pdf}
    \caption{\label{fig:3D-parallel}数据并行、张量并行、流水并行结合的3D并行模式示意图}
\end{figure}

在数据并行中，由于模型的梯度本身就是分层计算的，因此可以通过重叠梯度计算和通信来实现计算与通信的并行优化，从而减少通信开销。在流水并行中，模型被划分为多个顺序执行的阶段，采用Micro-batch技术时，天然具有不同Micro-batch间的计算与通信的并行性。而对于张量并行，模型参数被划分到不同节点上，节点间需要频繁进行严格同步的参数交换以完成前向和反向传播，因此张量并行中的计算与通信并行是目前分布式训练任务中的一个重要挑战。

基于以上背景，本章针对分布式深度学习任务中的计算与通信并行优化问题，提出了一种针对张量并行的高效计算通信并行优化方法。我们通过Pytorch的上层接口实现了该方法，并在TP/DP混合并行的分布式训练任务中进行了性能验证。实验结果表明，本章所提出的方法能够有效减少Transformer模型的端到端训练时间，从而提升整体训练效率。

本章其他小结的内容安排如下：3.2节介绍了典型的线性平均一致问题并分别描述了同构带宽和异构带宽场景下的拓扑优化问题；3.3节介绍了本章提出的参数同步拓扑优化问题求解框架；3.4节通过不同场景下的实验对本章所提出方法的有效性进行验证分析；3.5节对本章内容进行了总结。

\section{数据并行/张量并行基础方法}\label{section3-2}
\subsection{Transformer模型结构} \label{section3-2-1}
Transformer模型\cite{vaswani2017attention}是一种基于自注意力机制的神经网络架构，广泛应用于自然语言处理和计算机视觉等领域。Transformer模型主要由编码器（Encoder）和解码器（Decoder）两部分组成，每部分包含多个相同的层。每个层主要包括多头自注意力机制（Multi-Head Self-Attention，MHA）和前馈神经网络（Feed-Forward Neural Network，FFN）两个子层。
图\autoref {fig:Transformer-overall}给出了Transformer模型的整体架构示意图。图\autoref {fig:Transformer-layer}给出了Transformer层的基本结构示意图。从图中可以看到Transformer层主要由多头自注意力机制和前馈神经网络两部分组成，我们以下简称这两部分为Attention层和MLP层。这两部分都以通用矩阵乘法（General Matrix Multiplication，GEMM）为核心计算操作，通过拆分其中的的矩阵乘法操作，可以实现Transformer模型的张量并行训练。

\begin{figure}[!h]
    \centering
    \subfigure[Transformer整体模型架构\cite{vaswani2017attention}\label{fig:Transformer-overall}]{
        \includegraphics[width=0.4\linewidth]{figure/chapter3/transformer.pdf}
    }
    \subfigure[Transformer层模型结构\cite{shoeybi2019megatron}\label{fig:Transformer-layer}]{
        \includegraphics[width=0.4\linewidth]{figure/chapter3/transformer_layer.pdf}
    }
    \caption{Transformer模型结构示意图}
    \label{fig:Transformer}
\end{figure}


\subsection{数据并行} \label{section3-2-2}
数据并行是一种常用的分布式训练策略，在数据并行中，每个计算节点持有整个模型的副本，并在划分的本地数据集上进行训练。各节点在本地计算梯度后，通过集合通信算子（如AllReduce）将本地梯度进行聚合，然后更新模型参数，以保持不同节点模型间的同步。 数据并行的主要优势在于其实现简单且易于扩展，适用于大多数神经网络模型。然而，随着模型规模的增大，单个节点可能无法容纳整个模型，导致数据并行面临存储和计算能力的限制。此外，数据并行中的通信开销也会随着节点数量的增加而显著上升，成为训练效率的瓶颈。因此，在实际应用中，数据并行常与其他并行方式（如张量并行、流水并行）结合使用，以充分利用计算资源并提升训练效率。数据并行训练模式的示意图如\autoref{fig:data_parallel}所示。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.5\linewidth]{figure/chapter3/DP.pdf}
    \caption{\label{fig:data_parallel}数据并行训练模式示意图}
\end{figure}

\subsection{张量并行方法：Megatron}\label{subsection3-2-3}

张量并行是一种通过在多个计算节点间划分神经网络模型的权重矩阵来实现分布式训练的方法。Megatron-LM（以下简称Megatron）\cite{shoeybi2019megatron}是NVIDIA提出的一种高效的张量并行训练框架，专门用于大规模Transformer模型的训练。Megatron-LM将Transformer模型的权重矩阵在行或列维度上进行划分，通过AllReduce操作确保划分前后的数学等效性，从而实现了高效的分布式训练。
Megatron基于两种形式的矩阵分块乘法实现张量并行：列并行（Column Parallel，CP）与行并行（Row Parallel，RP）。列并行将权重矩阵按列划分，每个节点持有权重矩阵的一部分列，并在本地计算输入矩阵与该部分列的乘积，随后通过AllGather操作将各节点的计算结果进行汇总，得到完整的输出矩阵。行并行则将权重矩阵按行划分，同时将输入矩阵对应地按列切分，每个节点持有权重矩阵的一部分行，并在本地计算该部分行与输入矩阵对应部分列的乘积。随后通过AllReduce操作将各节点的计算结果进行汇总，得到完整的输出矩阵。
两种矩阵分块乘法的示意图分别如图\autoref{fig:figure/chapter3/column_parallel}和图\autoref{fig:figure/chapter3/row_parallel}所示。
\begin{figure}[!h]
    \centering
    \subfigure[列并行\label{fig:figure/chapter3/column_parallel}]{
        \includegraphics[width=0.45\linewidth]{figure/chapter3/column_parallel.pdf}
    }
    \subfigure[行并行\label{fig:figure/chapter3/row_parallel}]{
        \includegraphics[width=0.45\linewidth]{figure/chapter3/row_parallel.pdf}
    }
    \caption{矩阵分块乘法原理示意图}
    \label{fig:Transformer}
\end{figure}

以2节点张量并行组为例，针对MLP层，Megatron提出了如\autoref{fig:TP-MLP}所示的TP部署方法。
\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.6\linewidth]{figure/chapter3/TP-MLP.pdf}
    \caption{\label{fig:TP-MLP}Megatron张量并行MLP层部署示意图\cite{shoeybi2019megatron}}
\end{figure}
MLP层的第一部分包括一个线性层和一个激活层，分别进行通用矩阵乘法计算和GeLU激活函数的非线性计算，其计算公式为
$$
Y=\text{GeLU}\left( XA \right) ,
$$
对首个线性层执行列并行，即将权重矩阵按列分块，有
$$
A=\left[ A_1,\,\,A_2 \right] ,
$$
这一并行策略可以让GeLU独立作用在每个分块矩阵乘法的输出上，因此，以上列并行模块计算过程可表示为
$$
\left[ Y_1,\,\,Y_2 \right] =\left[ \text{GeLU}\left( XA_1 \right) ,\,\,\text{GeLU}\left( XA_1 \right) \right] ,
$$
列并行能够消除同步点，各分块矩阵乘法的计算结果可以异步地开始GeLU计算。

MLP的第二部分为一个线性层和一个Dropout层，分别进行矩阵乘法计算和Dropout非线性计算，其计算公式为
$$
Z=\text{Dropout}\left( YB \right) ,
$$
对第二部分线性层执行行并行，即将权重矩阵按行分块，相应的，输入矩阵按列分块，有
$$
B=\left[ \begin{array}{c}
	B_1\\
	B_2\\
\end{array} \right] ,\,\,Y=\left[ Y_1,\,\,Y_2 \right] ,
$$
以上行并行模块计算过程可表示为
$$
Z=\text{Dropout}\left( Y_1B_1+Y_2B_2 \right) ,
$$
由于Dropout为非线性函数，不满足可加性，因此，在进入Dropout计算之前需要一次同步操作，通过AllReduce通信聚合分块矩阵。而这种将行并行接续在列并行之后的部署组合，可以省去列并行模块后的AllGather通信，从而降低了通信开销。

类似地，以2节点张量并行组为例，针对Attention层，Megatron提出了如\autoref{fig:TP-Attention}所示的TP部署方法。
\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.6\linewidth]{figure/chapter3/TP-Attention.pdf}
    \caption{\label{fig:TP-Attention}Megatron张量并行Attention层部署示意图\cite{shoeybi2019megatron}}
\end{figure}

在Attention层中，Megatron利用多头注意力机制MHA所固有的并行性，将与键（Key, K）、查询（Query, Q）和值（Value, V）相关的矩阵乘法采用列并行的方式执行，使得每个注意力头对应的矩阵乘法均可在单个节点上独立完成。这种划分方式能够将每个注意力头的参数与计算任务分布到不同节点上执行，在自注意力后的输出线性层中的矩阵乘法则采用行并行的方式执行，它能够直接接收并行注意力层的输出，无需在节点之间进行通信。
这种方法在MLP层与Attention层中均实现了两个矩阵乘法的融合，有效消除了中间的同步点，从而提升了整体的可扩展性。最终，在一个完整的Transformer层中，所有矩阵乘法的计算仅需在前向传播中执行两次AllReduce操作，在反向传播中执行两次AllReduce操作，分别由\autoref{fig:TP-MLP}和\autoref{fig:TP-Attention}中的$f$运算符和$g$运算符表示。完整的Transformer层TP示意图如\autoref {fig:TP-Transformer-layer}所示。
\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.6\linewidth]{figure/chapter3/TP-comm.pdf}
    \caption{\label{fig:TP-Transformer-layer}Megatron张量并行Transformer层部署示意图\cite{shoeybi2019megatron}}
\end{figure}

在Transformer语言模型中，嵌入层的维度为隐藏层大小H与词汇表大小V的乘积。由于现代语言模型的词表规模通常在数万个词元量级，因此，Megatron也对嵌入层进行了并行化。然而，在Transformer语言模型中，输出嵌入层与输入嵌入层共享权重，这就要求同时对两者进行修改。
这里采用类似列并行的方法，将输入嵌入层的权重矩阵沿着词汇表维度进行划分，由于每个分区仅包含部分嵌入表，因此在输入嵌入层之后需要一次AllReduce操作。对于输出嵌入层，则直接采用执行并行的矩阵乘法运算得到分块的logits输出后，再通过一次AllGather操作，将结果送入交叉熵损失函数计算得到loss的方法。



\subsection{TP/DP混合并行模式}\label{subsection3-2-4}

TP/DP混合并行模式结合了数据并行和张量并行的优势，以应对大规模模型训练中的计算和内存需求。在这种模式下，模型首先通过张量并行在多个节点间划分权重矩阵，然后在每个节点上使用数据并行处理不同的数据批次。这样，每个节点持有模型的一个子集，并在本地数据上进行训练，同时通过AllReduce操作同步参数和梯度。该模式下，所有节点被组织成多个数据并行组和张量并行组，每个节点位于一个数据并行组和一个张量并行组中。在实际系统中部署时，考虑到不同并行模式的通信开销，一般将同一个张量并行组内的节点放在单个服务器内，以利用机内GPU间的高速链路降低通信开销，数据并行组内的节点则进行跨机通信。\autoref{fig:DP-TP}给出了512 GPU集群的8×64 TP/DP混合并行模式的示意图。在该图中，共有64个服务器，每个服务器内的8个GPU构成一个张量并行组，每个服务器内相同编号的GPU构成一个数据并行组，TP的维度为8，DP的维度为64，和一个分组内的GPU数量相对应。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.6\linewidth]{figure/chapter3/TP-DP.pdf}
    \caption{\label{fig:DP-TP}TP/DP混合并行模式示意图}
\end{figure}

\section{基于算子拆分调度的计算通信并行方法}
本节针对Transformer模型和Megatron张量并行方法，基于算子拆分调度的方式提出了一种计算通信并行优化方法。我们考虑如\autoref{fig:transformer_main}所示的核心Transformer模型结构，对嵌入层和Transformer层分别设计了计算通信并行方案。
针对TP/DP混合并行模式，我们结合本章提出的TP计算通信并行方法和现有的DP计算通信并行方法，使用Pytorch提供的接口实现了高效的TP/DP混合并行框架。
本节内容安排如下：3.3.1节介绍嵌入层计算通信并行方法；3.3.2节介绍Transformer层计算通信并行方法；3.3.3节介绍TP/DP计算通信并行方案；3.3.4节对所提出的计算通信并行方案进行了理论评估。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.2\linewidth]{figure/chapter3/transformer_main.pdf}
    \caption{\label{fig:transformer_main}Transformer模型核心结构示意图}
\end{figure}

\subsection{嵌入层计算通信并行方法}
我们对模型嵌入层的执行流程进行了优化设计。在Megatron的基准实现中，嵌入层采用串行执行模式，即依次执行词嵌入（Word Embedding）、全局同步AllReduce通信、位置嵌入（Position Embedding）、相加（Add）与随机失活（Dropout）操作。该模式下计算单元在等待通信完成期间处于空闲状态，造成了计算资源的浪费，由此我们提出了改进的并行流方案：将原有串行流程拆分为计算流（Computation Stream）和通信流（Communication Stream）。
改进方案的核心思想是在计算流中执行完词嵌入后在通信流中发起AllReduce通信操作，同时在计算流中并行地计算位置嵌入。这种设计使得耗时的通信操作与位置嵌入的计算实现了并行，从而有效地隐藏了通信开销。
\autoref{fig:embedding_method}展示了嵌入层基线方案和优化后计算通信并行方案的执行流程。

% \begin{figure}[!htbp]   
%     \centering
%     \subfigure[基线方案\label{fig:figure/chapter3/column_parallel}]{
%         \includegraphics[width=0.15\linewidth]{figure/chapter3/embedding_base.pdf}
%     }
%     \subfigure[计算通信并行方案\label{fig:figure/chapter3/row_parallel}]{
%         \includegraphics[width=0.3\linewidth]{figure/chapter3/embedding_optim.pdf}
%     }
%     \caption{嵌入层基线方案和计算通信并行方案的执行流程示意图}
%     \label{fig:embedding_method}
% \end{figure}
\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.5\linewidth]{figure/chapter3/embedding_method.pdf}
    \caption{\label{fig:embedding_method}嵌入层基线方案和计算通信并行方案的执行流程示意图}
\end{figure}



\subsection{Transformer层计算通信并行方法}

Transformer层基线方案和优化后计算通信并行方案的执行流程如\autoref{fig:transformer_method}所示。
\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=1\linewidth]{figure/chapter3/transformer_method.pdf}
    \caption{\label{fig:transformer_method}Transformer层基线方案和计算通信并行方案的执行流程示意图}
\end{figure}

% \begin{figure}[!htbp]   
%     \centering
%     \subfigure[基线方案\label{fig:transformer_baseline}]{
%         \includegraphics[width=0.4\linewidth]{figure/chapter3/transformer_baseline.pdf}
%     }
%     \subfigure[计算通信并行方案\label{fig:transformer_optim}]{
%         \includegraphics[width=0.55\linewidth]{figure/chapter3/transformer_optim.pdf}
%     }
%     \caption{Transformer层基线方案和计算通信并行方案的执行流程示意图}
%     \label{fig:transformer_method}
% \end{figure}

% \begin{figure}[!htbp]   
%     \centering
%     \includegraphics[width=0.6\linewidth]{figure/chapter3/transformer_baseline.pdf}
%     \caption{\label{fig:transformer_baseline}Transformer层基线方案执行流程示意图}
% \end{figure}

% \begin{figure}[!htbp]   
%     \centering
%     \includegraphics[width=0.8\linewidth]{figure/chapter3/transformer_optim.pdf}
%     \caption{\label{fig:transformer_optim}Transformer层计算通信并行方案执行流程示意图}
% \end{figure}

\autoref{fig:transformer_method}左侧子图所示的基线方案采用Megatron提出的列并行与行并行交替进行的经典张量并行方法。Transformer包含Attention层和MLP层，采用列并行与行并行两种计算模式。在前向传播中，输入依次经过多头注意力（MHA）或GeLU激活函数、矩阵乘法（MatMul）、非线性变换（Non-Linear）、矩阵乘法，最终通过AllReduce进行梯度同步。反向传播过程中，误差梯度经由梯度矩阵乘法、非线性变换的梯度、梯度矩阵乘法，并通过AllReduce实现梯度的全局聚合。该方案在Attention层和MLP层后均需进行同步全局通信（AllReduce），由于通信操作与计算串行执行，设备在通信期间处于等待状态，造成了计算资源的空闲与延迟。

\autoref{fig:transformer_method}右侧子图展示了Transformer层的计算和通信并行优化方案，该方案对前向传播和反向传播进行了针对性的优化设计。

在前向传播阶段，将原有的的AllReduce操作拆解为ReduceScatter和AllGather操作，并将对应的行并行和列并行矩阵计算相应地进行拆分。行并行的计算拆分后可以和ReduceScatter通信并行，列并行的计算拆分后可以和AllGather通信并行，从而实现计算与通信的并行。\autoref{fig:transformer_optim_forward_detail}展示了Transformer层前向传播部分基线方案与对应计算通信并行优化方案的具体实现细节。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=1\linewidth]{figure/chapter3/transformer_optim_forward_detail.pdf}
    \caption{\label{fig:transformer_optim_forward_detail}Transformer层前向传播基线方案与计算通信并行方案示意图}
\end{figure}

在反向传播过程中，每一层的梯度计算由两部分组成，分别为损失函数关于输入的梯度（Input Gradient）计算和损失函数关于该层模型参数的梯度（Weight Gradient），在同一层中这两部分计算是互不依赖的，仅依赖于上一层的输入梯度。前向与反向的模型计算图如\autoref{fig:transformer_computation_graph}所示。因此，我们将反向传播中的梯度计算进行了上述拆分，从而进一步优化了计算与通信的并行性。

\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=0.8\linewidth]{figure/chapter3/transformer_computation_graph.pdf}
    \caption{\label{fig:transformer_computation_graph}前向传播与反向传播计算图}
\end{figure}

在反向传播阶段，将两个并行层之间用于梯度同步的AllReduce操作同样拆解为ReduceScatter和AllGather操作，并将它们分别与对应的梯度计算过程并行执行：ReduceScatter与输入梯度计算并行，AllGather与权重梯度（Weight Gradient）计算并行。这样在保留梯度同步功能的同时最大化地隐藏了通信开销，从而提高了整体训练效率。

\subsection{TP/DP计算通信并行方案}

TP/DP混合并行是分布式训练中常用的设置，我们针对TP/DP混合并行模式提出了一种高效的计算通信并行方案。该方案结合了前述的TP计算通信并行方法和现有的DP计算通信并行方法，实现了TP/DP混合并行模式下的高效训练。针对DP组间的通信，采用Goyal等人\cite{goyal2017accurate}提出的方法，利用梯度桶（Gradient Bucket）机制将模型梯度按层划分为多个子集，并在计算每个子集的梯度时，异步地发起对应的AllReduce通信操作，从而实现模型反向传播与DP组间通信的并行，该方法可以通过通过Pytorch的DDP（DistributedDataParallel）接口便捷地实现。TP/DP混合并行模式下，仅TP计算通信并行的计算通信流时间线和TP与DP均计算通信并行的计算通信流时间线如\autoref{fig:TP-DP_Overlap}所示。
\begin{figure}[!htbp]   
    \centering
    \includegraphics[width=1\linewidth]{figure/chapter3/TP-DP_Overlap.pdf}
    \caption{\label{fig:TP-DP_Overlap}TP/DP混合并行模式下的计算通信并行时间线示意图}
\end{figure}

\subsection{计算通信并行方案理论评估}
本节对提出的计算通信并行方案进行了理论评估，其中相关参数定义如\autoref{tab:param_def}所示。
\begin{table}[htbp]
  \centering
  \caption{参数及参数含义}
  \label{tab:param_def}

  \makebox[\textwidth][c]{%
    \begin{tabular}{>{\centering\arraybackslash}p{0.2\textwidth}
                    >{\centering\arraybackslash}p{0.4\textwidth}}
      \toprule
      参数 & 参数含义 \\
      \midrule
      $B$ & 总批次（Global batch size） \\
      $S$ & 序列长度（Sequence Length） \\
      $H$ & 隐藏维度（Hidden Size） \\
      $V$ & 词汇表长度（Vocabulary Size） \\
      $M$ & MLP 层中间隐藏维度 \\
      $N_{\mathrm{TP}}$ & 张量并行（TP）组节点数 \\
      $N_{\mathrm{DP}}$ & 数据并行（DP）组节点数 \\
      $B_w$ & 物理链路带宽 \\
      \bottomrule
    \end{tabular}%
  }
\end{table}

嵌入层中，AllReduce通信的数据量为
$
V_{\mathrm{AR}}^{\mathrm{emb}}
= \frac{B \times S \times H}{N_{\mathrm{DP}}}.
$
采用Ring AllReduce算法时，其通信时间为
\begin{equation}
  T_{\mathrm{AR}}^{\mathrm{emb}}
  = \frac{B \times S \times H}{N_{\mathrm{DP}} \, B_w}
    \times \frac{2\left(N_{\mathrm{TP}} - 1\right)}{N_{\mathrm{TP}}}.
\end{equation}

设 $T_{\mathrm{PE}}$ 为位置嵌入（Positional Embedding）操作时间，则嵌入层中可被计算掩盖的通信时间为
\begin{equation}
  T_{\mathrm{lap}}^{\mathrm{emb}}
  = \min\!\left(
      \frac{B \times S \times H}{N_{\mathrm{DP}} \, B_w}
      \times \frac{2\left(N_{\mathrm{TP}} - 1\right)}{N_{\mathrm{TP}}},
      \; T_{\mathrm{PE}}
    \right).
\end{equation}

\medskip

对于Transformer Block，在模型前向传播阶段，Attention层的行并行计算可与ReduceScatter通信并行，其数据量为
$
V_{\mathrm{RP}}^{\mathrm{att}}
= \frac{B \times S \times H}{N_{\mathrm{DP}} \, N_{\mathrm{TP}}}.
$
设$T_{\mathrm{ARP}}$为Attention层单步行并行矩阵计算时间;
MLP层的列并行计算可与AllGather通信并行，其数据量为
$
V_{\mathrm{CP}}^{\mathrm{mlp}}
= \frac{B \times S \times M H}{N_{\mathrm{DP}} \, N_{\mathrm{TP}}}.
$
$T_{\mathrm{MCP}}$为MLP层单步列并行矩阵计算时间。
同理，设$T_{\mathrm{MRP}}$为MLP层单步行并行矩阵计算时间，$T_{\mathrm{ACP}}$为Attention层单步列并行矩阵计算时间。
综上，一个Transformer Block前向传播中计算通信可并行执行的时间为
\begin{equation}
\begin{aligned}
T_{\mathrm{lap}}^{\mathrm{fwd}}
= (N_{\mathrm{TP}} - 1)\!\Big[
& \min\!\left(
\frac{3B S H}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{ACP}}
\right)
+ \min\!\left(
\frac{B S H}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{ARP}}
\right) \\
& + \min\!\left(
\frac{B S M H}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{MCP}}
\right)
+ \min\!\left(
\frac{B S H}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{MRP}}
\right)
\Big].
\end{aligned}
\end{equation}

在模型的反向传播阶段，Transformer Block列并行输入梯度的计算与ReduceScatter通信并行执行，而列并行权重梯度的计算与 AllGather 通信并行执行。
对于 Attention 层，其列并行的输入梯度与权重梯度的通信量分别为
$V_{\mathrm{input}}^{\mathrm{att}}
= \frac{3BSH}{N_{\mathrm{DP}}N_{\mathrm{TP}}}$ 与
$V_{\mathrm{weight}}^{\mathrm{att}}
= \frac{3BH^2}{N_{\mathrm{DP}}N_{\mathrm{TP}}}$；
对于 MLP 层，其列并行的输入梯度与权重梯度的通信量分别为
$V_{\mathrm{input}}^{\mathrm{mlp}}
= \frac{BSMH}{N_{\mathrm{DP}}N_{\mathrm{TP}}}$ 与
$V_{\mathrm{weight}}^{\mathrm{mlp}}
= \frac{BMH^2}{N_{\mathrm{DP}}N_{\mathrm{TP}}}$。
设 $T_{\mathrm{AI}}$ 与 $T_{\mathrm{AW}}$ 分别表示 Attention 层单步输入梯度与权重梯度的计算时间，
$T_{\mathrm{MI}}$ 与 $T_{\mathrm{MW}}$ 分别表示 MLP 层单步输入梯度与权重梯度的计算时间，
则在一个 Transformer Block中，反向传播阶段计算与通信能够并行执行的时间为
\begin{equation}
\begin{aligned}
T_{\mathrm{lap}}^{\mathrm{bwd}}
= (N_{\mathrm{TP}} - 1)\!\Big[
& \min\!\left(
\frac{3BSH}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{AI}}
\right)
+ \min\!\left(
\frac{3BH^2}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{AW}}
\right) \\
& + \min\!\left(
\frac{BSMH}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{MI}}
\right)
+ \min\!\left(
\frac{BMH^2}{N_{\mathrm{DP}} N_{\mathrm{TP}} B_w}\frac{2}{N_{\mathrm{TP}}},
\, T_{\mathrm{MW}}
\right)
\Big].
\end{aligned}
\end{equation}








\section{实验评估}
本节对提出的计算通信并行方法进行了实验验证，并与Megatron的基准实现进行了对比分析。本文实验在算力自由平台提供的GPU计算节点上完成。实验节点配置8张NVIDIA L40S，每张L40S拥有48GB显存，GPU间通过PCIe 4.0×16接口与主机系统相连，构成多GPU PCIe互连架构。软件层面均使用Pytorch提供的接口实现。

\subsection{实验设置}
实验测试了不同参数设置的Transformer模型，不同规格模型的参数表如\autoref{tab:transformer_model_para}所示。实验采用TP/DP混合并行模式，使用单节点内8张GPU进行训练，其中TP维度设置为4，DP维度设置为2。

\begin{table}[!htbp]
    \centering
    \caption{不同规格Transformer模型参数设置}
    \label{tab:transformer_model_para}
    \begin{tabular}{cccccc}
        \toprule
        模型规格 & Layers & Hidden size & Attention heads & Vocabulary size & Max sequence length\\
        \midrule
        一 & 12 & 2048 & 32 & 5000 & 1024\\
        二 & 24 & 3072 & 48 & 5000 & 1024\\
        三 & 36 & 4096 & 64 & 5000 & 1024\\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{实验结果与分析}

\autoref{tab:time_speedup}展示了三种规格下基线和改进方法的实跑参数和结果，三种模型规格下均能达到1.25×以上的百分比。\autoref{tab:overlap_fwd_bwd}展示了基线和改进方法实跑具体计算和通信占比与并行时长，其中全模型通信时长可以分为TP通信和DP通信两部分。DP通信仅为关于模型梯度的AllReduce。对于TP通信部分，前向传播过程包括12个Transformer层中的24个AllReduce、嵌入层AllReduce以及输出层ReduceScatter，反向传播过程包括12个Transformer层中的24个AllReduce、嵌入层AllReduce以及输出层ReduceScatter。

\begin{table}[htbp]
\centering
\caption{不同模型规格的训练时间与加速比对比}
\label{tab:time_speedup}
% \small
\begin{tabular}{c c c c c}
\toprule
模型规格 & Global batch size
& 基线时间(s) & 改进时间(s) & 加速比 \\
\midrule
一 & 64 & 2.347 & 1.876 & 1.25$\times$ \\
二 & 32 & 2.562 & 1.993 & 1.29$\times$ \\
三 & 16 & 1.758 & 1.402 & 1.25$\times$ \\
\bottomrule
\end{tabular}
\end{table}


\begin{table}[htbp]
\centering
\caption{不同模型规格下前向与反向阶段计算通信并行效果对比（单位：ms）}
\label{tab:overlap_fwd_bwd}
% \small
\scalebox{0.9}{
\begin{tabular}{c c c c c c c c}
\toprule
模型规格 &  &  
& 基线计算 & 基线通信 & 基线总时长 & 改进总时长 & 并行时长 \\
\midrule
\multirow{5}{*}{一}
& \multirow{2}{*}{前向}
& 嵌入层
& 5.784 & 18.433 & 24.217 & 21.040 & 3.177 \\
&& Transformer层
& 51.613 & 50.585 & 102.198 & 86.621 & 15.577 \\
\cmidrule(lr){2-8}
& 反向
& Transformer层
& 116.736 & 51.543 & 168.279 & 134.597 & 33.807 \\
\cmidrule(lr){2-8}
& \multicolumn{2}{c}{DP 通信}
& -- & 81.553 & 81.553 & 11.320 & 74.233 \\
\cmidrule(lr){2-8}
& \multicolumn{2}{c}{全模型}
& 1370.162 & 977.478 & 2347.640 & 1875.932 & 471.708 \\
\midrule
\multirow{5}{*}{二}
& \multirow{2}{*}{前向}
& 嵌入层
& 5.558 & 19.461 & 25.019 & 21.991 & 3.028 \\
&& Transformer层
& 58.819 & 50.238 & 109.057 & 88.651 & 20.405 \\
\cmidrule(lr){2-8}
& 反向
& Transformer层
& 124.394 & 55.629 & 180.023 & 149.021 & 31.002 \\
\cmidrule(lr){2-8}
& \multicolumn{2}{c}{DP 通信}
& -- & 158.463 & 158.463 & 11.199 & 147.264 \\
\cmidrule(lr){2-8}
& \multicolumn{2}{c}{全模型}
& 1491.636 & 1070.983 & 2562.619 & 1993.885 & 568.734 \\
\midrule
\multirow{5}{*}{三}
& \multirow{2}{*}{前向}
& 嵌入层
& 9.817 & 12.798 & 22.615 & 18.744 & 3.871 \\
&& Transformer层
& 43.918 & 29.256 & 73.174 & 67.378 & 5.796 \\
\cmidrule(lr){2-8}
& 反向
& Transformer层
& 73.080 & 32.412 & 105.492 & 97.300 & 8.192 \\
\cmidrule(lr){2-8}
& \multicolumn{2}{c}{DP 通信}
& -- & 261.418 & 261.418 & 21.937 & 239.481 \\
\cmidrule(lr){2-8}
& \multicolumn{2}{c}{全模型}
& 967.714 & 791.237 & 1758.951 & 1402.000 & 356.560 \\
\bottomrule
\end{tabular}
}
\end{table}


图5.4为模型Transformer层前向传播Profiling时间线对比，使用模型规格一的场景进行分析，注意力层和MLP层之间的AllReduce拆分为ReduceScatter和AllGather，并将注意力层的行并行部分计算与ReduceScatte并行，将MLP层的列并行计算与AllGather并行。切分后的计算时间总体比原计算时间长，可能是因为计算矩阵变小，计算核利用率下降。在TP维度为4的情况下，点对点传输的Ring AllReduce共需要六步，ReduceScatter和AllGather部分能够各掩盖三步划分后的矩阵计算。时间线上展示了并行的计算和通信，与预期结果一致。

\begin{figure}[!htbp]   
    \centering
    \subfigure[基线方案]{
        \includegraphics[width=1\linewidth]{figure/chapter3/profiling_forward_base.pdf}
    }
    \subfigure[计算通信并行方案]{
        \includegraphics[width=1\linewidth]{figure/chapter3/profiling_forward_optim.pdf}
    }
    \caption{Transformer层前向传播基线方案和计算通信并行方案的Profiling时间线对比}
    \label{fig:transformer_method}
\end{figure}

图5.8为模型Transformer层反向传播Profiling时间线对比，使用模型规格一的场景进行分析， AllReduce解耦为ReduceScatter和AllGather，并分别将ReduceScatter与列并行的输入梯度计算分片并行，将AllGather与列并行的权重梯度计算直接并行。切分后的计算时间总体比原计算时间长，可能是因为计算矩阵变小，计算核利用率下降。此外，DP的AllReduce也与TP的计算相并行。时间线上展示了并行的计算和通信，与预期结果一致。

\begin{figure}[!htbp]   
    \centering
    \subfigure[基线方案]{
        \includegraphics[width=1\linewidth]{figure/chapter3/profiling_backward_base.pdf}
    }
    \subfigure[计算通信并行方案]{
        \includegraphics[width=1\linewidth]{figure/chapter3/profiling_backward_optim.pdf}
    }
    \caption{Transformer层反向传播基线方案和计算通信并行方案的Profiling时间线对比}
    \label{fig:transformer_method}
\end{figure}



\section{本章小结}
本项目围绕大规模模型计算与通信并行优化展开了系统性研究，梳理了当前主流的混合并行范式与代表性技术，包括基于张量并行的Megatron、Collective Einsum，基于数据并行的DeepSpeed ZeRO，以及基于流水并行的Zero Bubble调度算法。在此基础上，针对Transformer模型，分别从推理与训练两个阶段提出了改进方案：通过在嵌入层和Transformer块中拆分通信算子、细粒度调度矩阵计算，实现计算与通信的并行，有效减少设备空闲时间；通过在反向传播中优化算子顺序和梯度同步策略，进一步掩盖通信开销，提高端到端吞吐量。

理论分析和实测结果表明，所提出的优化方案能够在不改变模型精度的前提下显著提升大模型分布式训练与推理的性能，具有良好的可扩展性和可移植性。未来工作可进一步探索跨节点异步通信机制、更细粒度流水调度及异构算力环境下的调优策略，以支撑百亿乃至万亿参数级模型的高效训练与推理。
